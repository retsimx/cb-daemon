[package]
name = "aa-link"
version.workspace = true
edition.workspace = true

[dependencies]
rustix = { version = "1", features = ["fs", "termios"] }
tokio = { version = "1", features = ["fs", "io-util", "time", "macros", "rt", "net"] }

[lints]
workspace = true
