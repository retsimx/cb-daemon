[package]
name = "aa-mailbox"
version.workspace = true
edition.workspace = true
description = "Northbound mailbox JSON message types and RegisterBank ↔ JSON converters"

[dependencies]
aa-registers = { path = "../aa-registers" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[lints]
workspace = true
